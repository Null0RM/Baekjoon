#include <stdio.h>

int main(void)
{
    int T;
    int x1, y1, x2, y2;
    int N;

    scanf("%d", &T);

    while (T--)
    {
        scanf("%d %d %d %d", &x1, &y1, &x2, &y2);
        scanf("%d", &N);
        while (N--)
        {
        }
    }
}